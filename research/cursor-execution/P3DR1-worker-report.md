# P3DR1 worker report — Detail artifact integrity and audio-affordance honesty

Status: implementation evidence returned; **no approval claimed**  
Packet: `docs/cursor-packets/P3DR1-detail-integrity.md`  
Composer findings: `research/cursor-execution/P3D-composer-review-result.json` (P2 content integrity + P2 fake Play)  
Model: `cursor-grok-4.5-high` (non-Fast)  
Date: 2026-08-13

## Exact changed paths

Write-scoped:

| Path | Change |
|---|---|
| `platform/apps/web/lib/content/detail-canonical-contract.ts` | Centralized learner-visible pins (vocab / sein pairs / Q&A / paths / stable stringify) |
| `platform/apps/web/lib/content/access.ts` | Runtime assert: golden pins, exact ID/kind/path set, array↔`byId` deep equality |
| `platform/apps/web/lib/content/detail-project.ts` | Projection uses contract gap/note/sein order; forms must match contract |
| `platform/apps/web/scripts/project-content.ts` | `assertLearnerDetailProjection` before writing `learner-details.json` |
| `platform/apps/web/lib/content/media-copy.ts` | `PRONUNCIATION_APPROVED_PENDING_ACTIVATION_EXPLANATION` (`approved; activation pending`) |
| `platform/apps/web/components/audio/PronunciationControl.tsx` | Never `canPlay`; all states disabled with honest status copy |
| `platform/tests/web/p3d-details.test.ts` | Adversarial per-family + byId-only tamper tests; no leaked values in errors |
| `platform/tests/web/p3d-details-ui.test.ts` | Synthetic `approved` cannot render enabled Play; pending-review copy exact |
| `research/cursor-execution/P3DR1-worker-report.md` | This report |

Also regenerated by prebuild (unchanged content hash expected): `platform/apps/web/generated/learner-details.json` (and sibling projection artifacts via project script).

Not edited: plans, register, packet docs, content/learning packages, resources, archive, samples, media, governance.

## P2 fixes

### 1 — Runtime detail assert binds canonical content + array↔byId

- Added `detail-canonical-contract.ts` with pinned published learner-visible fields only (no review plurals, TTS paths, assertion values).
- Pins include: `der Architekt` / `die Architektin` / stem+`-in` / empty plurals + gap copy; seven ordered `(person, form)` pairs for `verb:sein`; informal Q&A question + three answer realizations; exact IDs/kinds/canonical paths.
- `assertLearnerDetailProjection` now:
  - requires exact `representativesById` key set;
  - deep-equals each `representatives[]` row with `representativesById[id]` via `stableStringifyDetailValue`;
  - re-asserts `byId` records;
  - fails closed on golden mismatches without echoing unsafe values.
- `project-content.ts` asserts the generated projection **before** write.

**Adversarial tests** (`p3d-details.test.ts`): independently mutate vocabulary, verb, QA (both array+byId), and byId-only `verb:sein` form; each throws; error messages contain none of the leaked invented strings / forms.

### 2 — No fake enabled pronunciation control

- `resolveAudioControl` always returns `canPlay: false` for `pending-review`, `approved`, and `missing`.
- UI always renders disabled / `aria-disabled` / `tabIndex={-1}` “Pronunciation unavailable” with status copy.
- Pending-review explanation remains exact: `technical candidate exists; qualified listening approval pending`.
- Synthetic approved uses exact `approved; activation pending`.
- UI test proves synthetic `{ state: "approved", assetId: "…" }` never renders “Play pronunciation”, `<audio>`, or enabled control.

## Artifact hash / leak scan

- Path: `platform/apps/web/generated/learner-details.json`
- `representativeCount`: **3**
- Bytes: **9543**
- SHA-256: `17a7fe2038b1be434ff5864e6492fc1c0ba6e6e9b765120b0fd31f6a90e357bd`
- Assert + tests: no review plurals, no `.mp3` / `media/generated` / `candidate-needs-listening-review` / assertion/source keys; media remains `pending-review` with `assetId: null` in the honest projected artifact.

## Command results

From `platform/`:

| Command | Exit | Summary |
|---|---:|---|
| `npm run check` | 0 | typecheck + typecheck:web + typecheck:web-tests + test + validate:publication + test:web; **15** files / **376** tests; publication `VALIDATION_OK`; web **9** files / **88** tests |
| `npm run build:web` | 0 | Next 16.3.0; assert-before-write in project; **●** 2 lessons + **●** 23 activities + **●** 3 detail routes SSG |
| `npm run audit:prod` | 0 | **0** vulnerabilities |
| `npm run smoke:web-routes` | 0 | **26/26** checks PASS (prior SSG/hubs/search + detail 200/raw-colon/404) |

## Remaining gaps (honest)

- **Content:** Plurals for `Architekt` / `Architektin` still unpublished — gap UI only.
- **Media:** Matching TTS remains listening-review pending; no public approved-audio URL contract or playback wiring yet — approved UI stays disabled by design until that exists.
- **Alternate lexeme:** `lex:architektin` remains related person-form only (not its own detail route).
- **Other hub details / Review / Add-to-Review / recorder / mastery:** still next-phase (P4+).
- Hub list routes remain **ƒ Dynamic**; lesson/activity/detail representatives remain SSG.
- Orchestrator owns approval / register closure; this report does not claim them.

## Honesty

No approval claim. No commit/push. No German invention, review-plural promotion, candidate-media exposure/copy, resources/archive/samples/plans/register edits, or security/publication gate weakening. Prior P3A/P3B/P3C gates preserved (check/build/audit/smoke green).
