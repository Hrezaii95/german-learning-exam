# Cursor C0 execution record — 2026-08-07

Status: blocked before artifact; no Cursor output credited  
Packet: `docs/cursor-packets/C0-schema-and-validation.md`

## Requested configuration

- implementation model: `cursor-grok-4.5-high`;
- Fast variant: prohibited and not used;
- intended review model after implementation: `composer-2.5`, High reasoning, `fast=false`, non-Max;
- workspace: project root;
- execution: headless print mode with trusted workspace and bounded write ownership.

## Verified facts

- The Cursor CLI accepted the configured API key and reported the account as logged in.
- The local model list contained `cursor-grok-4.5-high`, `cursor-grok-4.5-high-fast`, `composer-2.5`, and `composer-2.5-fast`.
- Process command-line inspection proved that the C0 attempt selected exactly `cursor-grok-4.5-high`, not Fast.
- The full C0 request produced no console response and no `platform/` files before the parent timeout; its orphaned child was stopped.
- A second minimal read-only streamed request using the same exact Grok model also produced no event or response before timeout; its orphaned child was stopped.
- No Cursor Agent process remained after cleanup.

## Gate consequence

C0 remains pending. Composer review was not launched because no implementation artifact exists. Retry only after a minimal `cursor-grok-4.5-high` request returns successfully. Do not switch to Fast, Composer for implementation, or another model without an owner decision.

No credential value, token, or secret-derived fingerprint is stored in this record.

## Resolution — 2026-08-08

The CLI block above remains valid historical evidence, but it is no longer the active execution path. Official Cursor SDK 1.0.27 completed the same work without changing the requested model policy:

- implementation: `grok-4.5`, `effort=high`, `fast=false`;
- review: `composer-2.5`, `effort=high`, `fast=false`;
- SDK probe: finished successfully before implementation;
- C0R1 implementation run: `run-9770b83e-5c23-42e9-9079-8f8162139f5e`;
- Composer re-review run: `run-b6c854ae-80e6-476e-8e87-0278e511edb2`, verdict APPROVE with zero P0/P1;
- C0R2 hardening run: `run-31500b21-e1af-40f7-856f-f915c3488648`.

Independent post-C0R2 verification passed `npm run typecheck`, 41 Vitest contract tests, and `npm run validate:fixture` (`VALIDATION_OK`). The implementation remains confined to the packet-owned `platform/` paths and does not publish lesson content.

## C0R3 review remediation — 2026-08-08

The mandatory Codex code review found two P1 bypass classes after C0R2: references resolved by existence without checking entity kind, and runtime enum/token discriminants accepted malformed values. Packet `docs/cursor-packets/C0R3-contract-bypass-remediation.md` records the required remediation.

Two SDK implementation attempts used exactly `grok-4.5`, `effort=high`, `fast=false` and failed before any edit because Cursor returned `Invalid User API Key`:

- `run-0a55c325-ba38-4d62-8de7-c278448aede4`;
- `run-7892f658-be02-489a-ab64-834e114ab5bd`.

No alternative Cursor model or Fast variant was used. Codex applied the bounded remediation directly under its implementation responsibility. Independent checks then passed typecheck, 48 contract tests, the valid fixture (`VALIDATION_OK`), and a static machine-path/credential-pattern scan. Final reviewer promotion remains recorded separately in the plan baton.
