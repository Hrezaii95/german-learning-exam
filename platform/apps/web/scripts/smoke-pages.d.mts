/**
 * ESM declaration sibling for smoke-pages.mjs (.d.mts pairs with .mjs).
 * Covers assertBasePrefixedNextAssets for root + web-tests typecheck.
 */
export function assertBasePrefixedNextAssets(
  html: string,
  label: string,
  pagesBase?: string,
): void;
