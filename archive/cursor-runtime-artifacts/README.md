# Cursor runtime artifacts

This directory quarantines incidental files emitted by Cursor or its MCP integrations. They are not application source, learning content, QA evidence, or reusable configuration.

- `browserstack-mcp-NUL.log` was written at the repository root using the Windows-reserved name `NUL`; it contains BrowserStack MCP startup log lines only and was moved here unchanged on 2026-08-07.
